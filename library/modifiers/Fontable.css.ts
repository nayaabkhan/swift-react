import { atoms } from '@themes/atoms.css'

export const fontVariants = {
  heading: atoms({
    fontSize: 'base',
    fontWeight: 'bold',
  }),
  subheading: atoms({
    fontSize: 'small',
    color: 'text.muted',
  }),
}
